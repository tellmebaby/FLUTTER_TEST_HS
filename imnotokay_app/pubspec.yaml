name: your_project_name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  sensors_plus: ^2.0.1
  cupertino_icons: ^1.0.6  # 중복된 항목을 제거한 후 하나만 남깁니다.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/back.jpg